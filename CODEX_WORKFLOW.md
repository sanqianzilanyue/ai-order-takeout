# Codex 任务工作流

本仓库采用轻量、可追溯的任务分支与交接机制。处理任务时遵守以下长期规则。

## 任务分支

- 每个任务使用独立分支，命名格式为 `task_xxx_简短名`。
- `xxx` 使用递增的三位编号，例如 `task_001_status-handoff`。
- 新任务开始前先确认当前分支、HEAD、远端关系和工作区状态，避免在错误历史上创建分支。

## 状态记录

- 任务开始时更新 `status/current.json`。
- 任务出现关键状态变化时同步更新该文件，包括执行中、等待用户、报错和完成。
- 状态值只允许使用：
  - `working`：任务正在执行。
  - `waiting_user`：需要用户确认、授权或接手操作。
  - `error`：任务发生错误或被阻塞。
  - `done`：任务已经完成。
- 状态文件应记录当前任务、分支、步骤、简要消息和带时区的更新时间。

## Handoff 记录

- 任务完成、暂停、报错或需要用户接手时，在 `docs/handoff/` 下新增 handoff 记录。
- handoff 的内容和命名遵循 `docs/handoff/README.md`。
- handoff 不得记录密码、验证码、Cookie、支付信息、完整地址或其他敏感数据。

## 暂存与提交

- 提交前必须先运行 `git status`，确认工作区和暂存区范围。
- 只 stage 当前任务直接相关的文件，使用明确的文件路径。
- 禁止默认使用 `git add .`；只有用户明确要求且确认范围安全时才可使用等价的批量暂存方式。
- 暂存后使用 `git diff --cached --name-status` 检查提交内容。

## 保护用户工作区

- 用户原有的未提交修改和未跟踪文件必须保留。
- 不擅自 stash、删除、覆盖、还原或修改用户已有内容。
- 如本任务与现有修改发生冲突，应停止并向用户说明，不能自行丢弃任何一方的内容。

## 远端与 Pull Request

- `origin` 指向用户自己的 fork，任务分支 push 到 `origin`。
- 原始仓库保留为 `upstream`，用于获取上游状态和作为 Pull Request 的目标仓库。
- 需要审查时，从用户 fork 的任务分支创建目标为 `upstream` 主分支的 Pull Request。
- 不自行 merge Pull Request；合并必须由用户或获明确授权的维护者执行。
